let obj={
    abc:9,
    6:40,
    5:42,
    7:90
}
console.log(obj["abc"]);
console.log(obj[5]);

// Bracket notation needs to tell that key is a string if it is not a number else you  can directly write it